<UserControl x:Class="DevLab.Wpf.Tests.Mvvm.MvvmView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <Grid Margin="20">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <TextBlock Text="MVVM task list: add a nonblank task, select it, then remove it." TextWrapping="Wrap"/>
  <DockPanel Grid.Row="1" Margin="0,12">
   <Button DockPanel.Dock="Right" Content="Add task" Command="{Binding AddCommand}" Margin="8,0,0,0"/>
   <TextBox Text="{Binding Draft, UpdateSourceTrigger=PropertyChanged}"/>
  </DockPanel>
  <ListBox Grid.Row="2" ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask, Mode=TwoWay}" DisplayMemberPath="Title"/>
  <Button Grid.Row="3" Content="Remove selected" Command="{Binding RemoveCommand}" Margin="0,12,0,0"/>
 </Grid>
</UserControl>
