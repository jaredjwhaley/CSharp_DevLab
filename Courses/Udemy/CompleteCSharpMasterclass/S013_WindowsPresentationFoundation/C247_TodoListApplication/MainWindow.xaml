<Window x:Class="C247_TodoListApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:C247_TodoListApplication"
        mc:Ignorable="d"
        Title="Todo App" Height="450" Width="400"
        ResizeMode="NoResize"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
        BorderBrush="{DynamicResource {x:Static SystemColors.WindowFrameBrushKey}}"
        BorderThickness="0">
    
    <Grid Margin="0"
          Background="{DynamicResource {x:Static SystemColors.WindowFrameBrushKey}}">
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition x:Name="row_textEntry" Height="*" />
            <RowDefinition x:Name="row_output" Height="100" />
            <RowDefinition x:Name="row_button" Height="50" />
        </Grid.RowDefinitions>
        
        <ScrollViewer Grid.Column="0" Grid.Row="0"
                      Background="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}"
                      Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                      Margin="3,3,3,3"
                      VerticalScrollBarVisibility="Auto">

            <StackPanel x:Name="TodoListStackPanel">
                <!-- Procedurally filled with TextBlocks for each Todo List Item
                <TextBlock x:Name="TodoListItemTextBlock"
                           Background="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}"
                           Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
                -->
            </StackPanel>
        </ScrollViewer>
        
        <TextBox x:Name="TodoListItemTextBox"
            Grid.Column="0" Grid.Row="1"
            AcceptsReturn="True" TextWrapping="Wrap"
            VerticalScrollBarVisibility="Auto"
            BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
            SelectionBrush="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"
            SelectionTextBrush="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"
            Background="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}"
            Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
            AutomationProperties.HelpText="Input new todo list items here..."
            Margin="3,0,3,3"
            Padding="1"/>

        <Button x:Name="CreateTodoListItemButton"
                Grid.Column="0" Grid.Row="2"
                Margin="3,0,3,3"
                Content="Create Todo List Item"
                Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}"
                BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}"
                Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                Click="CreateTodoListItemButton_Click"/>
    </Grid>
</Window>